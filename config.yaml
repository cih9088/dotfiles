#######################
# apps & libs
#######################

pkg-config:

patch:

cmake:

zlib:

bzip2:

unzip:

gzip:

xz:

gnutls:

gnupg:

libx11:

terminfo:
  mode: local

zsh:

prezto:

neovim:

tmux:

wget:

tree:

fd:

rg:

tldr:

up:

jq:

sox:

tcpdump:

#######################
# dots
#######################

bins:
  mode: local

configs:
  mode: local

tpm:
  mode: local

#######################
# envs
#######################

asdf:
  mode: local

python:
  version: 3.8.10

golang:
  version: 1.17.5

rust:
  version: 1.65.0

nodejs:
  version: 19.0.0

lua:
  version: 5.4.3

python-env:
  virtualenv:

#######################
# plugins
#######################

tmux-plugins:
  mode: local

neovim-plugins:
  mode: local

neovim-providers:
  mode: local
