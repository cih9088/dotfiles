#######################
# envs
#######################

mise:

perl:
  mode: local
  version: 5.36.0

python:
  mode: local
  version: 3.9.22

python-env:
  virtualenv:

golang:
  mode: local
  version: 1.23.8

rust:
  mode: local
  version: 1.76.0

nodejs:
  mode: local
  version: "17.9.1" # glibc 2.27 compatible

lua:
  mode: local
  version: 5.4.3

#######################
# apps & libs
#######################

zsh:

prezto:

neovim:

tmux:

wget:

tree:

fd:

rg:

tldr:

up:

jq:

sox:

tcpdump:

#######################
# dots
#######################

bins:
  mode: local

configs:
  mode: local

tpm:
  mode: local

tmux-plugins:
  mode: local

neovim-plugins:
  mode: local

neovim-providers:
  mode: local
